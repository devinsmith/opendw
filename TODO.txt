op_51 extracts data from ES segment, but it's not clear where ES is setup

More drawing routines (sub_CF8)
There is still some UI glitches with the viewport (e.g. water displaying instead of land).

Run script (Section 3, 1706 -> 0x1E1)
op_7C is the start of a random encounter (I think) but we don't match up with dragon.com for some reason.
 -- Check that op_30 is correctly populating variables.

- Calculation of jump address in 28B0 (BX) is not always correct...
  but not every caller even seems to care. This should be based on input parameter
